metrics:
  - name: slurm_node_status
    scope: node
    instances:
      - compute*
      - visu*
    labels_only: true
    labels:
      node: $node_id
      partition: $partition
      status: $status
