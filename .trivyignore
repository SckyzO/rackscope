# Trivy ignore file — CVEs accepted as low-risk or without available fix
#
# Format: one CVE-ID per line, with comment explaining the decision.
#
# No active exceptions: dompurify (formerly CVE-2024-48910 via monaco-editor)
# is now patched through the `overrides.dompurify` pin in frontend/package.json,
# which resolves dompurify to a fixed version without downgrading monaco-editor.
